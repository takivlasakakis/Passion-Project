

<h1 align="center">display a list of all links</h1>

<ul>
<% @result.each do |hash| %>
	<li>
	<a href="<%= hash[:url]%>"><%= hash[:headline] %> </a> 
	</li>
<% end %>
</ul>

<!-- AJAX a save route next to each item, next challenge -->